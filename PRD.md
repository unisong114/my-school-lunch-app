# PRD: 급식 배틀 - 학교 급식 조회 앱

## 1. 개요

- **앱 제목**: 급식 배틀 - 학교 급식 조회 앱
- **문서 목적**: 이 문서(PRD)는 앱이 무엇을, 왜, 어떤 흐름으로 동작해야 하는지를 정의하는 제품 요구사항의 단일 기준(single source of truth)입니다. 구체적인 구현 방식은 [`TRD.md`](./TRD.md)에서 다룹니다.
- **한 줄 소개**: NEIS 공개 API를 활용해 사용자가 원하는 학교와 날짜 범위의 급식 메뉴(중식 기준)를 조회하는 웹 애플리케이션이며, 동일한 급식 데이터를 MCP(Model Context Protocol) 클라이언트가 도구 호출로 조회할 수 있는 MCP 서버도 함께 제공합니다.

## 2. 개발 목적

- 학생, 학부모, 교직원이 특정 학교의 급식 정보를 빠르고 간편하게 확인할 수 있도록 합니다.
- NEIS에서 제공하는 [공개 API](./data/openapi.json)를 활용해 신뢰할 수 있는 급식 데이터를 제공합니다.
- 학교 검색부터 결과 확인까지 3단계로 단순화한 사용자 흐름을 통해 누구나 쉽게 사용할 수 있는 경험을 제공합니다.
- AI 에이전트 등 MCP 클라이언트가 표준화된 도구 호출을 통해 학교 검색과 급식 조회 기능을 재사용할 수 있도록 합니다.

## 3. 대상 사용자

| 사용자 유형 | 주요 요구 |
|-------------|-----------|
| 학생 | 오늘/이번 주 우리 학교 급식 메뉴를 빠르게 확인 |
| 학부모 | 자녀 학교의 날짜별 급식 메뉴 확인 |
| 교직원 | 특정 기간의 급식 식단을 조회하고 참고 |
| MCP 클라이언트(AI 에이전트 등) | 학교 검색·급식 조회 기능을 MCP 도구 호출로 재사용 |

## 4. 사용자 흐름 (3단계)

앱은 다음 3단계의 사용자 흐름으로 구성합니다.

1. **학교 검색 및 선택**: 사용자가 학교 이름의 일부만 입력해 학교를 검색하고, 검색 결과 목록에서 원하는 학교를 선택합니다.
2. **날짜 범위 선택**: 급식을 조회할 시작일과 종료일(날짜 범위)을 선택합니다.
3. **급식 결과 표시**: 선택한 학교와 날짜 범위에 대해 **중식(점심) 기준**의 날짜별 급식 메뉴를 표시합니다.

```mermaid
flowchart LR
    A[학교 부분 검색 및 선택] --> B[날짜 범위 선택]
    B --> C[중식 기준 날짜별 급식 결과 표시]
```

위 3단계 흐름은 웹 애플리케이션 사용자를 대상으로 합니다. MCP 클라이언트는 동일한 정보를 아래와 같이 도구 호출로 조회합니다.

```mermaid
flowchart LR
    D[학교 검색 도구 호출] --> E[학교 식별 정보 확인]
    E --> F[급식 조회 도구 호출]
    F --> G[중식 기준 날짜별 급식 결과 수신]
```

## 5. 기능 요구사항

### FR-1. 부분 학교명 검색
- 사용자는 학교 이름의 일부만 입력해 학교를 검색할 수 있다.
- 검색 결과는 학교명, 소재지 등 학교를 식별할 수 있는 정보와 함께 목록으로 표시된다.

### FR-2. 학교 선택
- 사용자는 검색 결과 목록에서 하나의 학교를 선택할 수 있다.
- 선택한 학교는 이후 급식 조회의 대상이 된다.

### FR-3. 날짜 범위 선택
- 사용자는 급식을 조회할 시작일과 종료일을 선택할 수 있다.
- 시작일은 종료일보다 이후일 수 없다.

### FR-4. 급식 조회 및 표시
- 사용자는 선택한 학교와 날짜 범위로 급식 조회를 요청할 수 있다.
- 조회 결과는 **중식 기준**으로 날짜별 급식 메뉴를 표시한다.
- 각 날짜의 메뉴 정보를 사용자가 읽기 쉬운 형태로 표시한다.

### FR-5. MCP 학교 검색 도구
- MCP 클라이언트는 학교 이름의 일부를 입력해 후보 학교를 검색하는 도구를 호출할 수 있다.
- 도구 응답은 후보 학교의 이름, 교육청 정보, 학교 식별 정보(학교 코드 등)를 포함한다.

### FR-6. MCP 급식 조회 도구
- MCP 클라이언트는 선택한 학교의 식별 정보와 시작일·종료일을 입력해 급식 조회 도구를 호출할 수 있다.
- 도구 응답은 `data/openapi.json`에 정의된 NEIS API를 사용해 조회한 **중식 기준** 날짜별 급식 정보를 포함한다.
- 기존 백엔드 API와 독립적으로 동작하며, MCP 클라이언트는 도구 목록을 조회해 제공되는 도구를 확인할 수 있다.

## 6. 예외 상황 (Edge Cases)

| 예외 상황 | 기대 동작 |
|-----------|-----------|
| **검색 결과 없음** | 입력한 학교명과 일치하는 학교가 없을 때, 결과가 없음을 알리는 안내 메시지를 표시한다. |
| **잘못된 날짜 범위** | 시작일이 종료일보다 이후이거나 유효하지 않은 날짜를 선택한 경우, 조회를 막고 오류를 안내한다. |
| **급식 정보 없음** | 선택한 학교와 날짜 범위에 급식 정보가 없을 때(예: 주말·공휴일·방학), 해당 날짜에 급식 정보가 없음을 안내한다. |
| **외부 API 오류** | NEIS API 호출 실패 또는 응답 지연 시, 사용자에게 일시적인 오류임을 알리고 재시도를 유도한다. |
| **MCP 도구 입력값 오류** | MCP 도구 호출의 입력값이 유효하지 않을 때(필수값 누락, 잘못된 날짜 형식 등), MCP 표준에 맞는 명확한 오류 응답을 반환하고 민감 정보(NEIS API 키 등)를 노출하지 않는다. |
| **MCP 검색/급식 정보 없음** | MCP 도구 호출 결과 학교 검색 결과나 급식 정보가 없을 때, 그 사실을 나타내는 오류 응답을 반환한다. |
| **MCP 외부 API 오류·지연** | MCP 서버가 NEIS API 호출에 실패하거나 응답이 지연될 때, 일시적인 오류임을 나타내는 오류 응답을 반환한다. |

## 7. 범위 밖 (Out of Scope)

- 조식·석식 등 중식 이외의 급식 구분 표시
- 급식 메뉴 비교/분석 등 부가 기능(후속 단계에서 다룸)
- 사용자 계정 및 개인화 기능

## 8. 인수 조건

- [ ] 학교 이름의 일부만으로 학교를 검색하고 선택할 수 있다.
- [ ] 날짜 범위를 선택할 수 있으며 잘못된 범위는 차단된다.
- [ ] 선택한 학교와 날짜 범위에 대해 중식 기준 날짜별 급식이 표시된다.
- [ ] 검색 결과 없음, 잘못된 날짜 범위, 급식 정보 없음 등 예외 상황이 적절히 처리된다.
- [ ] 기존 백엔드 API와 독립적으로 실행되는 MCP 서버가 있으며, MCP 클라이언트가 연결해 제공 도구 목록을 조회할 수 있다.
- [ ] 학교 이름 일부를 입력해 후보 학교의 이름·교육청·학교 식별 정보를 조회하는 MCP 도구가 제공된다.
- [ ] 선택한 학교와 시작일·종료일을 입력해 중식 기준 날짜별 급식 정보를 반환하는 MCP 도구가 제공된다.
- [ ] MCP 도구의 입력값 오류, 검색 결과 없음, 급식 정보 없음, NEIS API 오류·지연이 민감 정보 노출 없이 MCP 표준 오류 응답으로 처리된다.
