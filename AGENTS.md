# AGENTS.md

이 문서는 리포지토리 전체에서 작업하는 코딩 에이전트에게 적용됩니다. 하위
디렉터리에 더 구체적인 `AGENTS.md`가 있으면 해당 범위에서는 그 지침을
우선합니다.

## 프로젝트 개요

이 프로젝트는 NEIS 공개 API를 활용해 초중고 급식 메뉴를 조회하고 분석하는 웹
애플리케이션을 단계별로 구현하는 워크숍입니다. 현재 구조와 진행 단계는
`README.md` 및 `docs/`를 기준으로 파악하고, 요청된 범위에 필요한 파일만
변경합니다.

## 일반 작업 지침

- 사용자 요구사항과 인수 조건을 먼저 확인하고 기존 동작을 보존합니다.
- 이 문서보다 저장소의 기존 코드, 설정, 문서화된 규칙을 우선합니다.
- 관련 구현과 테스트를 살펴본 뒤 기존 패턴과 명명 규칙을 따릅니다.
- 문제의 원인을 해결하는 작고 집중된 변경을 만들고 관련 없는 리팩터링은
  피합니다.
- 현재 요구사항에 필요하지 않은 추상화, 확장 지점, 의존성을 미리 추가하지
  않습니다.
- 생성 파일, 외부 코드, 잠금 파일은 해당 도구나 의존성 변경이 요구할 때만
  수정합니다.

## Python

- 공개 함수와 모듈 경계에는 명확한 타입을 사용하고, 불필요한 `Any`와 타입
  검사 우회는 피합니다.
- 입력은 경계에서 검증하고, 구체적인 예외를 사용하며, 오류를 삼키거나
  성공처럼 보이는 기본값으로 대체하지 않습니다.
- 표준 라이브러리와 기존 의존성을 우선 사용합니다. 새 의존성은 필요성과
  유지보수 영향을 확인한 뒤 프로젝트의 기존 의존성 관리 방식으로 추가합니다.
- 코드는 작고 응집력 있게 유지하며 기존 포맷터, 린터, 타입 검사기 및 프레임워크
  관례를 따릅니다.

## TypeScript

- 엄격한 타입을 유지하고 `any`, 과도한 타입 단언, null 가능성의 무시를
  피합니다. 외부 데이터는 사용 전에 런타임에서 검증합니다.
- 오류를 명시적으로 처리하고 유용한 문맥을 보존합니다. 예외나 거부된 Promise를
  조용히 무시하지 않습니다.
- 기존 패키지와 플랫폼 API를 우선 사용합니다. 새 패키지는 필요한 경우에만
  프로젝트의 패키지 관리자를 통해 추가하고 잠금 파일을 함께 갱신합니다.
- 기존 컴포넌트, 유틸리티, 명명 및 모듈 경계를 재사용하고 중복 구현을
  만들지 않습니다.

## 테스트 및 유효성 검사

- 동작 변경에는 정상 경로와 중요한 실패·경계 사례를 다루는 테스트를 추가하거나
  수정합니다. 버그 수정에는 가능하면 회귀 테스트를 포함합니다.
- 변경 범위에 맞는 기존 테스트, 포맷팅, 린트, 타입 검사 및 빌드 검사를
  실행합니다.
- 실행 명령은 저장소의 설정 파일, 스크립트, `CONTRIBUTING.md` 및 CI에서
  확인합니다. 구성되지 않은 도구나 명령을 임의로 도입하거나 성공한 것으로
  간주하지 않습니다.
- 실행하지 못한 검사는 이유와 함께 명확히 알립니다.

## 보안

- 비밀, 자격 증명, 토큰, 개인 정보 또는 실제 운영 데이터를 코드, 로그, 테스트
  픽스처와 커밋에 포함하지 않습니다.
- 모든 외부 입력을 신뢰하지 말고 검증하며, 최소 권한과 안전한 기본값을
  사용합니다.
- 인증·인가를 우회하거나 민감한 오류 정보를 노출하지 않습니다.
- 취약점은 공개 이슈에 게시하지 않고 `SECURITY.md`의 신고 절차를 따릅니다.

## 문서화

- 공개 API, 설정, 사용자 흐름 또는 실행 방법이 바뀌면 관련 문서를 함께
  갱신합니다.
- 주석은 코드만으로 드러나지 않는 이유와 제약을 설명할 때만 간결하게
  작성하고, 구현과 어긋난 문서를 남기지 않습니다.

## 주의사항 및 가드레일

- 사용자 변경을 덮어쓰거나 범위 밖 파일을 되돌리지 않습니다.
- 파괴적인 명령, 데이터 삭제, 마이그레이션 및 호환성 파괴 변경은 명시적인
  요구와 충분한 검증 없이 수행하지 않습니다.
- 불확실한 요구사항이나 보안·데이터 손실 위험이 있는 결정은 추측하지 말고
  확인합니다.
- 테스트를 삭제하거나 완화해 실패를 숨기지 않으며, 오류를 무시하는 우회책을
  추가하지 않습니다.

## Git 커밋 및 Pull Request

- 커밋은 하나의 논리적 변경에 집중하고 `CONTRIBUTING.md`의 Conventional
  Commits 규칙을 따릅니다. 비밀, 생성 산출물, 관련 없는 변경을 포함하지
  않습니다.
- Pull Request는 작고 검토 가능하게 유지하며 변경 이유, 관련 이슈, 영향,
  수행한 검증과 남은 위험을 명시합니다.
- `.github/PULL_REQUEST_TEMPLATE.md`를 사용하고 해당 체크리스트를 정확하게
  작성합니다. 관련 이슈는 저장소 규칙에 맞게 연결합니다.
- 제출 전 자신의 diff를 검토하고 관련 CI 검사가 통과하도록 합니다.
